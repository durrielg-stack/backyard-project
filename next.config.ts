import type { NextConfig } from 'next'

const nextConfig: NextConfig = {
  // Allow the app to be installed as a PWA later via next-pwa
  // No rewrites needed yet — all routes are internal
}

export default nextConfig
